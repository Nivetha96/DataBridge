 [project]
 name = "databridge"
 version = "0.1.0"
 description = "Data integration toolkit"
 requires-python = ">=3.10"

 dependencies = [
     "click",
     "cryptography>=48.0.0",
     "pandas>=2.3.3",
     "paramiko",
]

 [project.scripts]
 databridge = "databridge.main:cli"

 [build-system]
 requires = ["hatchling"]
 build-backend = "hatchling.build"
